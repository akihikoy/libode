sudo apt-get -f install libglut3 libglut3-dev

2. BUILDING WITH AUTOTOOLS (Linux)
   $ sh autogen.sh
   x $ ./configure --with-double-precision
   $ ./configure --enable-double-precision
   $ make
   x make install

For ode-0.12 @2015-06-10
   $ ./configure --enable-double-precision --disable-asserts
   $ make
--disable-asserts: To avoid the error:
  ODE INTERNAL ERROR 1: assertion "*iret != i0" failed in cullPoints() [box.cpp:306]
  Abort (core dumped)



